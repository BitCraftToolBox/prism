// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::enemy_location_type::EnemyLocation;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct BulkReplaceEnemiesArgs {
    pub region_id: u8,
    pub rows: Vec<EnemyLocation>,
    pub total: u32,
}

impl From<BulkReplaceEnemiesArgs> for super::Reducer {
    fn from(args: BulkReplaceEnemiesArgs) -> Self {
        Self::BulkReplaceEnemies {
            region_id: args.region_id,
            rows: args.rows,
            total: args.total,
        }
    }
}

impl __sdk::InModule for BulkReplaceEnemiesArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `bulk_replace_enemies`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait bulk_replace_enemies {
    /// Request that the remote module invoke the reducer `bulk_replace_enemies` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`bulk_replace_enemies:bulk_replace_enemies_then`] to run a callback after the reducer completes.
    fn bulk_replace_enemies(
        &self,
        region_id: u8,
        rows: Vec<EnemyLocation>,
        total: u32,
    ) -> __sdk::Result<()> {
        self.bulk_replace_enemies_then(region_id, rows, total, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `bulk_replace_enemies` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn bulk_replace_enemies_then(
        &self,
        region_id: u8,
        rows: Vec<EnemyLocation>,
        total: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl bulk_replace_enemies for super::RemoteReducers {
    fn bulk_replace_enemies_then(
        &self,
        region_id: u8,
        rows: Vec<EnemyLocation>,
        total: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            BulkReplaceEnemiesArgs {
                region_id,
                rows,
                total,
            },
            callback,
        )
    }
}
