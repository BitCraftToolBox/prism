[package]
name = "prism-relay-module"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
#spacetimedb = { version = "2.8.1", features = ["unstable"] }
# needed for default string columns, unpin when it makes a real release
spacetimedb = { git = "https://github.com/clockworklabs/SpacetimeDB", rev = "d8f488b11c66b3bc3cc3ac59e987546890d7ea43", features = ["unstable"] }
url = "2.5.8"
serde_json = "1.0.150"

# Standalone workspace so it can be published / versioned independently.
[workspace]
